# Protect data directory from direct web access
Deny from all

# Allow specific public JSON files
<FilesMatch "^(board|events|partners|testimonials)\.json$">
    Order Deny,Allow
    Allow from all
    Satisfy Any
</FilesMatch>
